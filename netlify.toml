# =========================================================
# ===    ملف إعدادات Netlify (النسخة النظيفة والسريعة)   ===
# =========================================================

[build]
  # أمر البناء الذي يشغل المصنع (سكربت الفهرسة + جيكل)
  command = "bash build.sh"
  # المجلد الذي سيتم نشره
  publish = "_site"

# =========================================================
# === 🔗 قواعد التوجيه الذكي (Pretty URLs & SEO)      ===
# =========================================================

# 1. تحويل روابط العقارات من شكل معقد إلى شكل بسيط
# الزائر يرى: /property/flat-101
# السيرفر يخدم: /details.html?id=flat-101
[[redirects]]
  from = "/property/:id"
  to = "/details.html?id=:id"
  status = 200 # (200 تعني Rewrite: الرابط يبقى جميلاً في المتصفح)

# 2. تحويل روابط الطلبات
# الزائر يرى: /request/req-505
# السيرفر يخدم: /request-details.html?id=req-505
[[redirects]]
  from = "/request/:id"
  to = "/request-details.html?id=:id"
  status = 200

# 3. قاعدة احتياطية لصفحة 404 (اختياري ولكنه احترافي)
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
